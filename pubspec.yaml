name: dingtalk_bot_sender
description: The library send message with webhook url of dingtalk bot.
version: 1.0.0
homepage: https://www.example.com

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4

dependencies:
  crypto: ^3.0.1
  http: ^0.13.3
